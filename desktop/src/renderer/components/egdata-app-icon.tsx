import iconUrl from '../../../resources/icon.png'

export function EgdataAppIcon({ className }: { className: string }) {
  return <img className={className} src={iconUrl} alt="" aria-hidden="true" />
}
